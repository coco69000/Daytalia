name: iOS-ipa-build

on:
  workflow_dispatch:

jobs:
  build-ios:
    name: 🎉 iOS Build
    runs-on: macos-latest
    steps:
      - name: ⬇️ Checkout repository
        uses: actions/checkout@v4

      - name: ⚙️ Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          cache: true

      - name: 📦 Get Packages
        run: flutter pub get

      - name: 📂 Update Pods
        run: pod install --repo-update
        working-directory: ios

      - name: 🏗️ Build iOS unsigned IPA
        # On compile directement avec Xcode pour obtenir un .app sans signature, puis Sideloadly fera la signature finale sur Windows.
        run: |
          cd ios
          xcodebuild -workspace Runner.xcworkspace -scheme Runner -configuration Release -sdk iphoneos -derivedDataPath ../build/ios CODE_SIGNING_ALLOWED=NO CODE_SIGNING_REQUIRED=NO DEVELOPMENT_TEAM=""

      - name: 📦 Prepare unsigned IPA
        run: |
          rm -rf Payload Daytalia.ipa
          mkdir -p Payload
          cp -R build/ios/iphoneos/Runner.app Payload/
          zip -qry Daytalia-unsigned.ipa Payload

      - name: 🚀 Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Daytalia-unsigned-ipa
          path: Daytalia-unsigned.ipa
          retention-days: 3
