name: iOS-ipa-build

on:
  workflow_dispatch:

jobs:
  build-ios:
    name: 🎉 iOS Build
    runs-on: macos-latest
    env:
      TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
    steps:
      - name: ⬇️ Checkout repository
        uses: actions/checkout@v4

      - name: ✅ Check Apple team ID
        run: |
          if [ -z "$TEAM_ID" ]; then
            echo "APPLE_TEAM_ID secret is missing. Configure it in repository secrets before building iOS."
            exit 1
          fi

      - name: ⚙️ Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          cache: true

      - name: 📦 Get Packages
        run: flutter pub get

      - name: 📂 Update Pods
        run: pod install --repo-update
        working-directory: ios

      - name: 🏗️ Build iOS unsigned IPA
        # On produit un binaire iOS brut, puis Sideloadly fera la signature finale sur Windows.
        run: flutter build ios --release --no-codesign

      - name: 📦 Prepare unsigned IPA
        run: |
          rm -rf Payload Daytalia.ipa
          mkdir -p Payload
          cp -R build/ios/iphoneos/Runner.app Payload/
          zip -qry Daytalia-unsigned.ipa Payload

      - name: 🚀 Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Daytalia-unsigned-ipa
          path: Daytalia-unsigned.ipa
          retention-days: 3
